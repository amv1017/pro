{
  "name": "pro",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "wmr --port=3000 --host && process.env.NODE_ENV='development'",
    "build": "wmr build && rimraf dist\\static",
    "serve": "wmr serve --port=8080 --host"
  },
  "alias": {
    "react": "preact/compat",
    "react-dom": "preact/compat"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "wmr": "^3.7.2"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sass": "^1.53.0"
  }
}
